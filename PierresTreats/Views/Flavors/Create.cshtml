@{
  Layout = "_Layout";
}

@model PierresTreats.Models.Flavor

@using (Html.BeginForm())
{
  <div class="row g-3">
    @Html.LabelFor(model => model.Name, htmlAttributes: new { @class = "control-label col-auto" })
    <div class="col-auto">
      @Html.TextBoxFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" }})
    </div>
  </div>

  <div class="row g-3">
    @Html.Label("Select Treat")
    @Html.DropDownList("TreatId")
  </div>

    <div class="form-group">
      <div class="col-md-offset-2 col-md-10">
        <button type="submit" value="Create" class="btn btn-outline-secondary">Add Flavor</button>
      </div>
    </div>
}

<p>@Html.ActionLink("Show all Flavors", "Index")</p>
<h5>@Html.ActionLink("Main Menu", "Index", "Home")</h5>