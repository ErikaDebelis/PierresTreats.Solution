@{
  Layout = "_Layout";
}
@using PierresTreats.Models;


<h3>Treat Name</h3>

@if (@Model.Count == 0)
{
  <h3><i class="bi bi-exclamation-triangle"></i> No Treats have been added yet!<i class="bi bi-exclamation-triangle"></i></h3>
}
</ul>
  @foreach(Treat treat in Model)
  {
    <li>@Html.ActionLink($"{treat.Description}", "Details", new { id = treat.TreatId })</li>
  }
</ul>

<p>
  @Html.ActionLink("Add a new Treat", "Create") | 
  @Html.ActionLink("Add a new Flavor", "Create", "Flavors")
</p>
<h5>@Html.ActionLink("Main Menu", "Index", "Home")</h5>